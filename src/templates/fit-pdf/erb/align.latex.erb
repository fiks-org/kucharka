<%# AMSMath align environment %>
<%# label: optional wobject label %>
<%# todo: multiline labels %>
<% if meta?('label') && meta['label'].is_a?(String) %>\begin{align}
<%= r(content).strip %>

\label{<%= meta['label'] %>}
\end{align}
<% else %>\begin{align*}
<%= r(content).strip.gsub(/\n[ ]+/,"\n") %>

\end{align*}
<% end %>